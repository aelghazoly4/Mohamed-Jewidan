# MongoDB connection string (MongoDB Atlas or self-hosted)
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority

# Optional: database name (defaults to "wedding")
MONGODB_DB=wedding

# Secret key required to view/manage RSVPs at /admin
ADMIN_KEY=choose-a-long-random-password
